# --- Supabase (Auth + Database + Storage) ---
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# --- OpenAI (server-only; /api/ai/* for AI Discover parse/chat) ---
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Phase 1: product catalog is Supabase-only (SerpAPI removed from catalog path).
